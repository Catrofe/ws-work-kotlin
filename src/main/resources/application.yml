spring:
  datasource:
    password: root
    username: postgres
    url: jdbc:postgresql://localhost:5432/wswork
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: update
    open-in-view: false